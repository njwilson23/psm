
from setuptools import setup, find_packages

setup(name='psm',
      packages=find_packages(exclude="test*"),
      author="Nat Wilson",
      version='0.1a',
      description='Naive parameter space sampler',
      license='Contact me',
      )

